apiVersion: apps/v1
kind: Deployment
metadata:
  name: faps-deployment
spec:
  selector:
    matchLabels:
      app: faps
  replicas: 4
  template:
    metadata:
      labels:
        app: faps
    spec:
      containers:
      - name: faps
        image: ghalamif/faps:green
        resources:
          limits:
            memory: "128Mi"
            cpu: "500m"
        ports:
        - containerPort: 8080
