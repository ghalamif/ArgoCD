apiVersion: v1
kind: Service
metadata:
  name: base-app-service
spec:
  type: NodePort
  selector:
    app: base-app
  ports:
    - protocol: TCP
      port: 3200
      targetPort: 80
      nodePort: 30016
