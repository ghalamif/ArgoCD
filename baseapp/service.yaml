apiVersion: v1
kind: Service
metadata:
  name: base-app-service
spec:
  type: LoadBalancer
  selector:
    app: base-app
  ports:
    - protocol: TCP
      port: 3200
      targetPort: 3000
      nodePort: 3008
